<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="co.edu.unal.tutorship">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".presentation.ActivityListGeneral"></activity>
        <activity android:name=".presentation.ActivityInfoTutorship"></activity>
        <activity android:name=".presentation.ActivityCreateTutorship"></activity>
        <activity android:name=".presentation.ActivityConfirmation"></activity>
        <activity android:name=".presentation.ActivityLoggedUser"></activity>
        <activity android:name=".presentation.ActivityInfoTutor"></activity>
        <activity android:name=".presentation.ActivityLogIn"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>